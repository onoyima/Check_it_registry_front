// Security Analysis Service Tests
const SecurityAnalysisService = require('../../server/services/SecurityAnalysisService');

// Mock Database
const mockDatabase = {
  query: jest.fn(),
  selectOne: jest.fn(),
  insert: jest.fn(),
  logAudit: jest.fn(),
  generateUUID: jest.fn(() => 'test-uuid-123')
};

jest.mock('../../server/config', () => mockDatabase);

describe('SecurityAnalysisService', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('analyzeDeviceCheck', () => {
    const mockCheckData = {
      user_id: 'test-user-1',
      device_id: 'test-device-1',
      location: {
        latitude: 40.7128,
        longitude: -74.0060,
        accuracy: 10
      },
      device_info: {
        user_agent: 'Mozilla/5.0 Test Browser',
        platform: 'web',
        screen_resolution: '1920x1080'
      },
      network_info: {
        ip_address: '192.168.1.100',
        connection_type: '4g'
      }
    };

    it('should analyze device check and return risk assessment', async () => {
      // Mock previous checks
      mockDatabase.query.mockResolvedValueOnce([\n        {\n          latitude: 40.7500,\n          longitude: -73.9857,\n          created_at: new Date(Date.now() - 3600000) // 1 hour ago\n        }\n      ]);\n\n      // Mock user device count\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 2 }]);\n\n      // Mock IP geolocation\n      mockDatabase.query.mockResolvedValueOnce([]);\n\n      const result = await SecurityAnalysisService.analyzeDeviceCheck(mockCheckData);\n\n      expect(result).toHaveProperty('risk_score');\n      expect(result).toHaveProperty('flags');\n      expect(result).toHaveProperty('recommendations');\n      expect(result).toHaveProperty('metadata');\n      expect(typeof result.risk_score).toBe('number');\n      expect(Array.isArray(result.flags)).toBe(true);\n    });\n\n    it('should detect location anomalies', async () => {\n      // Mock previous check from different continent\n      mockDatabase.query.mockResolvedValueOnce([\n        {\n          latitude: 51.5074, // London\n          longitude: -0.1278,\n          created_at: new Date(Date.now() - 1800000) // 30 minutes ago\n        }\n      ]);\n\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 1 }]);\n      mockDatabase.query.mockResolvedValueOnce([]);\n\n      const result = await SecurityAnalysisService.analyzeDeviceCheck(mockCheckData);\n\n      expect(result.risk_score).toBeGreaterThan(5);\n      expect(result.flags).toContain('location_anomaly');\n    });\n\n    it('should detect multiple device ownership patterns', async () => {\n      mockDatabase.query.mockResolvedValueOnce([]);\n      \n      // Mock high device count\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 15 }]);\n      mockDatabase.query.mockResolvedValueOnce([]);\n\n      const result = await SecurityAnalysisService.analyzeDeviceCheck(mockCheckData);\n\n      expect(result.flags).toContain('multiple_devices');\n      expect(result.risk_score).toBeGreaterThan(3);\n    });\n\n    it('should detect suspicious IP patterns', async () => {\n      mockDatabase.query.mockResolvedValueOnce([]);\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 1 }]);\n      \n      // Mock suspicious IP history\n      mockDatabase.query.mockResolvedValueOnce([\n        { user_id: 'other-user-1' },\n        { user_id: 'other-user-2' },\n        { user_id: 'other-user-3' }\n      ]);\n\n      const result = await SecurityAnalysisService.analyzeDeviceCheck(mockCheckData);\n\n      expect(result.flags).toContain('suspicious_ip');\n    });\n\n    it('should handle missing location data', async () => {\n      const checkDataWithoutLocation = {\n        ...mockCheckData,\n        location: null\n      };\n\n      mockDatabase.query.mockResolvedValueOnce([]);\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 1 }]);\n      mockDatabase.query.mockResolvedValueOnce([]);\n\n      const result = await SecurityAnalysisService.analyzeDeviceCheck(checkDataWithoutLocation);\n\n      expect(result.flags).toContain('no_location');\n      expect(result.risk_score).toBeGreaterThan(2);\n    });\n  });\n\n  describe('calculateLocationDistance', () => {\n    it('should calculate distance between two points', () => {\n      const point1 = { latitude: 40.7128, longitude: -74.0060 }; // NYC\n      const point2 = { latitude: 34.0522, longitude: -118.2437 }; // LA\n\n      const distance = SecurityAnalysisService.calculateLocationDistance(point1, point2);\n\n      expect(distance).toBeGreaterThan(3900); // Approximately 3944 km\n      expect(distance).toBeLessThan(4000);\n    });\n\n    it('should return 0 for same location', () => {\n      const point = { latitude: 40.7128, longitude: -74.0060 };\n\n      const distance = SecurityAnalysisService.calculateLocationDistance(point, point);\n\n      expect(distance).toBe(0);\n    });\n  });\n\n  describe('generateSecurityEvent', () => {\n    it('should create security event for high risk activity', async () => {\n      const eventData = {\n        user_id: 'test-user-1',\n        event_type: 'suspicious_location',\n        severity: 'high',\n        description: 'Location anomaly detected',\n        metadata: {\n          risk_score: 8,\n          flags: ['location_anomaly', 'rapid_movement']\n        },\n        ip_address: '192.168.1.100',\n        location: {\n          latitude: 40.7128,\n          longitude: -74.0060\n        }\n      };\n\n      mockDatabase.insert.mockResolvedValueOnce({ insertId: 1 });\n\n      await SecurityAnalysisService.generateSecurityEvent(eventData);\n\n      expect(mockDatabase.insert).toHaveBeenCalledWith(\n        'security_events',\n        expect.objectContaining({\n          id: 'test-uuid-123',\n          user_id: 'test-user-1',\n          event_type: 'suspicious_location',\n          severity: 'high',\n          description: 'Location anomaly detected'\n        })\n      );\n    });\n  });\n\n  describe('analyzeOwnershipTransfer', () => {\n    it('should analyze ownership transfer security', async () => {\n      const transferData = {\n        from_user_id: 'seller-123',\n        to_user_id: 'buyer-456',\n        device_id: 'device-789',\n        transfer_location: {\n          latitude: 40.7128,\n          longitude: -74.0060\n        },\n        ip_address: '192.168.1.100'\n      };\n\n      // Mock seller's previous transfers\n      mockDatabase.query.mockResolvedValueOnce([\n        { id: 'transfer-1' },\n        { id: 'transfer-2' }\n      ]);\n\n      // Mock buyer's device count\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 1 }]);\n\n      // Mock device history\n      mockDatabase.query.mockResolvedValueOnce([\n        { from_user_id: 'previous-owner' }\n      ]);\n\n      const result = await SecurityAnalysisService.analyzeOwnershipTransfer(transferData);\n\n      expect(result).toHaveProperty('risk_score');\n      expect(result).toHaveProperty('flags');\n      expect(result).toHaveProperty('recommendations');\n      expect(typeof result.risk_score).toBe('number');\n    });\n\n    it('should detect frequent seller pattern', async () => {\n      const transferData = {\n        from_user_id: 'frequent-seller',\n        to_user_id: 'buyer-456',\n        device_id: 'device-789',\n        ip_address: '192.168.1.100'\n      };\n\n      // Mock many previous transfers\n      const manyTransfers = Array.from({ length: 12 }, (_, i) => ({ id: `transfer-${i}` }));\n      mockDatabase.query.mockResolvedValueOnce(manyTransfers);\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 1 }]);\n      mockDatabase.query.mockResolvedValueOnce([]);\n\n      const result = await SecurityAnalysisService.analyzeOwnershipTransfer(transferData);\n\n      expect(result.flags).toContain('frequent_seller');\n      expect(result.risk_score).toBeGreaterThan(5);\n    });\n  });\n\n  describe('analyzePaymentSecurity', () => {\n    it('should analyze recovery service payment security', async () => {\n      const paymentData = {\n        user_id: 'test-user-1',\n        device_id: 'stolen-device-123',\n        amount: 50.00,\n        currency: 'USD',\n        payment_method: 'card',\n        ip_address: '192.168.1.100',\n        location: {\n          latitude: 40.7128,\n          longitude: -74.0060\n        }\n      };\n\n      // Mock user's payment history\n      mockDatabase.query.mockResolvedValueOnce([\n        { amount_paid: 100, created_at: new Date(Date.now() - 86400000) }\n      ]);\n\n      // Mock device report details\n      mockDatabase.selectOne.mockResolvedValueOnce({\n        report_type: 'stolen',\n        created_at: new Date(Date.now() - 3600000),\n        status: 'open'\n      });\n\n      const result = await SecurityAnalysisService.analyzePaymentSecurity(paymentData);\n\n      expect(result).toHaveProperty('risk_score');\n      expect(result).toHaveProperty('flags');\n      expect(result).toHaveProperty('payment_recommendation');\n    });\n\n    it('should detect suspicious payment patterns', async () => {\n      const paymentData = {\n        user_id: 'test-user-1',\n        device_id: 'device-123',\n        amount: 200.00,\n        currency: 'USD',\n        ip_address: '192.168.1.100'\n      };\n\n      // Mock recent multiple payments\n      const recentPayments = Array.from({ length: 5 }, (_, i) => ({\n        amount_paid: 50 + i * 10,\n        created_at: new Date(Date.now() - i * 3600000)\n      }));\n      mockDatabase.query.mockResolvedValueOnce(recentPayments);\n\n      // Mock very recent device report\n      mockDatabase.selectOne.mockResolvedValueOnce({\n        report_type: 'stolen',\n        created_at: new Date(Date.now() - 300000), // 5 minutes ago\n        status: 'open'\n      });\n\n      const result = await SecurityAnalysisService.analyzePaymentSecurity(paymentData);\n\n      expect(result.flags).toContain('multiple_recent_payments');\n      expect(result.flags).toContain('immediate_payment');\n    });\n  });\n\n  describe('getRiskLevel', () => {\n    it('should return correct risk levels', () => {\n      expect(SecurityAnalysisService.getRiskLevel(0)).toBe('very_low');\n      expect(SecurityAnalysisService.getRiskLevel(2)).toBe('low');\n      expect(SecurityAnalysisService.getRiskLevel(4)).toBe('medium');\n      expect(SecurityAnalysisService.getRiskLevel(7)).toBe('high');\n      expect(SecurityAnalysisService.getRiskLevel(9)).toBe('very_high');\n    });\n  });\n\n  describe('generateRecommendations', () => {\n    it('should generate appropriate recommendations based on flags', () => {\n      const flags = ['location_anomaly', 'multiple_devices', 'suspicious_ip'];\n      \n      const recommendations = SecurityAnalysisService.generateRecommendations(flags);\n\n      expect(Array.isArray(recommendations)).toBe(true);\n      expect(recommendations.length).toBeGreaterThan(0);\n      expect(recommendations.some(r => r.includes('location'))).toBe(true);\n      expect(recommendations.some(r => r.includes('device'))).toBe(true);\n    });\n\n    it('should return default recommendations for no flags', () => {\n      const recommendations = SecurityAnalysisService.generateRecommendations([]);\n\n      expect(Array.isArray(recommendations)).toBe(true);\n      expect(recommendations.length).toBeGreaterThan(0);\n    });\n  });\n\n  describe('Error Handling', () => {\n    it('should handle database errors gracefully', async () => {\n      const checkData = {\n        user_id: 'test-user-1',\n        device_id: 'test-device-1',\n        location: { latitude: 40.7128, longitude: -74.0060 },\n        network_info: { ip_address: '192.168.1.100' }\n      };\n\n      mockDatabase.query.mockRejectedValueOnce(new Error('Database connection failed'));\n\n      await expect(SecurityAnalysisService.analyzeDeviceCheck(checkData))\n        .rejects.toThrow('Database connection failed');\n    });\n\n    it('should handle invalid location data', async () => {\n      const checkData = {\n        user_id: 'test-user-1',\n        device_id: 'test-device-1',\n        location: { latitude: 'invalid', longitude: 'invalid' },\n        network_info: { ip_address: '192.168.1.100' }\n      };\n\n      mockDatabase.query.mockResolvedValueOnce([]);\n      mockDatabase.query.mockResolvedValueOnce([{ device_count: 1 }]);\n      mockDatabase.query.mockResolvedValueOnce([]);\n\n      const result = await SecurityAnalysisService.analyzeDeviceCheck(checkData);\n\n      expect(result.flags).toContain('invalid_location');\n    });\n  });\n});