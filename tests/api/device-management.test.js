// Device Management API Tests
const request = require('supertest');
const app = require('../../server/app');
const jwt = require('jsonwebtoken');

describe('Device Management API', () => {
  let userToken, adminToken, testUserId, testDeviceId;

  beforeAll(async () => {
    // Generate test tokens
    userToken = jwt.sign(
      { id: 'test-user-1', role: 'user' },
      process.env.JWT_SECRET,
      { expiresIn: '1h' }
    );
    
    adminToken = jwt.sign(
      { id: 'test-admin-1', role: 'admin' },
      process.env.JWT_SECRET,
      { expiresIn: '1h' }
    );

    testUserId = 'test-user-1';
    testDeviceId = 'test-device-new';
  });

  describe('POST /api/device-management/register', () => {
    it('should register a new device successfully', async () => {
      const deviceData = {\n        category: 'mobile_phone',\n        brand: 'Apple',\n        model: 'iPhone 14',\n        imei: '111111111111111',\n        serial: 'TEST123SERIAL',\n        purchase_date: '2023-01-15',\n        purchase_location: 'Apple Store',\n        purchase_price: 999.99\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/register')\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(deviceData)\n        .expect(201);\n\n      expect(response.body.success).toBe(true);\n      expect(response.body.device).toHaveProperty('id');\n      expect(response.body.device.brand).toBe('Apple');\n      expect(response.body.device.model).toBe('iPhone 14');\n      expect(response.body.device.status).toBe('unverified');\n      \n      testDeviceId = response.body.device.id;\n    });\n\n    it('should reject duplicate IMEI registration', async () => {\n      const deviceData = {\n        category: 'mobile_phone',\n        brand: 'Samsung',\n        model: 'Galaxy S22',\n        imei: '123456789012345', // Existing IMEI from test data\n        serial: 'DUPLICATE123'\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/register')\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(deviceData)\n        .expect(400);\n\n      expect(response.body.error).toContain('already registered');\n    });\n\n    it('should validate required fields', async () => {\n      const incompleteData = {\n        category: 'mobile_phone',\n        brand: 'Apple'\n        // Missing required fields\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/register')\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(incompleteData)\n        .expect(400);\n\n      expect(response.body.error).toContain('required');\n    });\n\n    it('should require authentication', async () => {\n      const deviceData = {\n        category: 'mobile_phone',\n        brand: 'Apple',\n        model: 'iPhone 14'\n      };\n\n      await request(app)\n        .post('/api/device-management/register')\n        .send(deviceData)\n        .expect(401);\n    });\n  });\n\n  describe('GET /api/device-management/my-devices', () => {\n    it('should return user devices', async () => {\n      const response = await request(app)\n        .get('/api/device-management/my-devices')\n        .set('Authorization', `Bearer ${userToken}`)\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n      expect(Array.isArray(response.body.devices)).toBe(true);\n      expect(response.body.devices.length).toBeGreaterThan(0);\n      \n      const device = response.body.devices[0];\n      expect(device).toHaveProperty('id');\n      expect(device).toHaveProperty('brand');\n      expect(device).toHaveProperty('model');\n      expect(device).toHaveProperty('status');\n    });\n\n    it('should support pagination', async () => {\n      const response = await request(app)\n        .get('/api/device-management/my-devices?page=1&limit=1')\n        .set('Authorization', `Bearer ${userToken}`)\n        .expect(200);\n\n      expect(response.body.pagination).toHaveProperty('page', 1);\n      expect(response.body.pagination).toHaveProperty('limit', 1);\n      expect(response.body.pagination).toHaveProperty('total');\n    });\n  });\n\n  describe('GET /api/device-management/device/:id', () => {\n    it('should return device details for owner', async () => {\n      const response = await request(app)\n        .get('/api/device-management/device/test-device-1')\n        .set('Authorization', `Bearer ${userToken}`)\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n      expect(response.body.device).toHaveProperty('id', 'test-device-1');\n      expect(response.body.device).toHaveProperty('brand');\n      expect(response.body.device).toHaveProperty('model');\n    });\n\n    it('should deny access to non-owner', async () => {\n      // Create another user token\n      const otherUserToken = jwt.sign(\n        { id: 'other-user', role: 'user' },\n        process.env.JWT_SECRET,\n        { expiresIn: '1h' }\n      );\n\n      await request(app)\n        .get('/api/device-management/device/test-device-1')\n        .set('Authorization', `Bearer ${otherUserToken}`)\n        .expect(403);\n    });\n\n    it('should allow admin access', async () => {\n      const response = await request(app)\n        .get('/api/device-management/device/test-device-1')\n        .set('Authorization', `Bearer ${adminToken}`)\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n    });\n  });\n\n  describe('PUT /api/device-management/device/:id', () => {\n    it('should update device information', async () => {\n      const updateData = {\n        model: 'iPhone 14 Pro',\n        color: 'Space Black',\n        storage_capacity: '256GB'\n      };\n\n      const response = await request(app)\n        .put(`/api/device-management/device/${testDeviceId}`)\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(updateData)\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n      expect(response.body.device.model).toBe('iPhone 14 Pro');\n    });\n\n    it('should not allow updating restricted fields', async () => {\n      const updateData = {\n        imei: '999999999999999', // Should not be updatable\n        status: 'verified' // Should not be updatable by user\n      };\n\n      const response = await request(app)\n        .put(`/api/device-management/device/${testDeviceId}`)\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(updateData)\n        .expect(400);\n\n      expect(response.body.error).toContain('restricted');\n    });\n  });\n\n  describe('POST /api/device-management/device/:id/report', () => {\n    it('should create theft report', async () => {\n      const reportData = {\n        report_type: 'stolen',\n        description: 'Device stolen from my car',\n        occurred_at: '2023-12-01T10:00:00Z',\n        location: {\n          latitude: 40.7128,\n          longitude: -74.0060,\n          address: 'New York, NY'\n        }\n      };\n\n      const response = await request(app)\n        .post(`/api/device-management/device/${testDeviceId}/report`)\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(reportData)\n        .expect(201);\n\n      expect(response.body.success).toBe(true);\n      expect(response.body.report).toHaveProperty('id');\n      expect(response.body.report.report_type).toBe('stolen');\n      expect(response.body.report.status).toBe('open');\n    });\n\n    it('should prevent duplicate active reports', async () => {\n      const reportData = {\n        report_type: 'lost',\n        description: 'Another report'\n      };\n\n      await request(app)\n        .post('/api/device-management/device/test-device-2/report')\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(reportData)\n        .expect(400);\n    });\n  });\n\n  describe('GET /api/device-management/categories', () => {\n    it('should return device categories', async () => {\n      const response = await request(app)\n        .get('/api/device-management/categories')\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n      expect(Array.isArray(response.body.categories)).toBe(true);\n      expect(response.body.categories.length).toBeGreaterThan(0);\n      \n      const category = response.body.categories.find(c => c.id === 'mobile_phone');\n      expect(category).toBeDefined();\n      expect(category).toHaveProperty('name');\n      expect(category).toHaveProperty('validation_rules');\n    });\n  });\n\n  describe('POST /api/device-management/validate', () => {\n    it('should validate device data', async () => {\n      const deviceData = {\n        category: 'mobile_phone',\n        imei: '123456789012345',\n        serial: 'VALID123'\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/validate')\n        .send(deviceData)\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n      expect(response.body.validation).toHaveProperty('valid');\n      expect(response.body.validation).toHaveProperty('errors');\n    });\n\n    it('should detect invalid IMEI', async () => {\n      const deviceData = {\n        category: 'mobile_phone',\n        imei: '12345', // Invalid IMEI\n        serial: 'VALID123'\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/validate')\n        .send(deviceData)\n        .expect(200);\n\n      expect(response.body.validation.valid).toBe(false);\n      expect(response.body.validation.errors).toContain('IMEI');\n    });\n  });\n\n  describe('Device Security Features', () => {\n    it('should log device check with security data', async () => {\n      const checkData = {\n        imei: '123456789012345',\n        location: {\n          latitude: 40.7128,\n          longitude: -74.0060,\n          accuracy: 10\n        },\n        device_info: {\n          user_agent: 'Mozilla/5.0 Test Browser',\n          platform: 'web',\n          screen_resolution: '1920x1080'\n        },\n        network_info: {\n          ip_address: '192.168.1.100',\n          connection_type: '4g'\n        }\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/security-check')\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(checkData)\n        .expect(200);\n\n      expect(response.body.success).toBe(true);\n      expect(response.body.security_analysis).toHaveProperty('risk_score');\n      expect(response.body.security_analysis).toHaveProperty('flags');\n    });\n\n    it('should detect suspicious activity', async () => {\n      const suspiciousData = {\n        imei: '123456789012345',\n        location: {\n          latitude: 51.5074, // London - far from previous check\n          longitude: -0.1278,\n          accuracy: 10\n        },\n        device_info: {\n          user_agent: 'Different Browser',\n          platform: 'mobile'\n        }\n      };\n\n      const response = await request(app)\n        .post('/api/device-management/security-check')\n        .set('Authorization', `Bearer ${userToken}`)\n        .send(suspiciousData)\n        .expect(200);\n\n      expect(response.body.security_analysis.risk_score).toBeGreaterThan(5);\n      expect(response.body.security_analysis.flags).toContain('location_anomaly');\n    });\n  });\n});